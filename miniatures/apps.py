"""Miniatures app config."""

from django.apps import AppConfig


class MiniaturesConfig(AppConfig):
    """Miniatures app config."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "miniatures"
